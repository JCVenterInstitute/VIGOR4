; Effective configuration 20180824-114821

; source: commandline
output_directory = "/home/snettem/git/VIGOR4/src/test/resources/vigor4ReferenceOutput/flua"

; source: commandline
output_prefix = "flua"

; source: defaults
max_intron_size = "2500"

; source: defaults
exonerate_path = "/usr/bin/exonerate"

; source: commandline
locus_tag = "vigor_"

; source: commandline
verbose = "false"

; source: defaults
reference_database_path = "/home/snettem/git/VIGOR_DB/Reference_DBs"

; source: defaults
temporary_directory = "/home/snettem/output"

; source: defaults
alignment_module = "exonerate"

; source: defaults
min_missing_AA_size = "10"

; source: commandline
candidate_selection = "1"

; source: defaults
NTOverlap_offset = "30"

; source: commandline
frameshift_sensitivity = "1"

; source: commandline
complete_gene = "false"

; source: defaults
mature_pep_mincoverage = "50.0"

; source: defaults
stop_codon_search_window = "50"

; source: defaults
min_pseudogene_coverage = "80.0"

; source: defaults
min_seq_gap_length = "20"

; source: defaults
min_condensation = "10"

; source: defaults
mature_pep_minsimilarity = "40.0"

; source: defaults
min_pseudogene_identity = "70.0"

; source: defaults
AAOverlap_offset = "10"

; source: defaults
min_pseudogene_similarity = "80.0"

; source: defaults
min_exon_size = "30"

; source: commandline
circular_gene = "false"

; source: defaults
StartCodons = "[ATG]"

; source: defaults
leakystop_notFound_score = "80.0"

; source: defaults
mature_pep_minidentity = "25.0"

; source: defaults
start_codon_search_window = "50"

